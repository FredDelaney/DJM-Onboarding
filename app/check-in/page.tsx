'use client';
import {useState} from 'react';import {Check,ChevronDown,ChevronUp,ArrowRight} from 'lucide-react';import {PlayerShell,usePlayerContext,LoadingScreen} from '@/components/PlayerShell';import {supabase,weekStartISO} from '@/lib/supabase';
export default function CheckIn(){const ctx=usePlayerContext();const [availability,setAvailability]=useState('available');const [fitness,setFitness]=useState('fully_fit');const [notes,setNotes]=useState('');const [support,setSupport]=useState('');const [details,setDetails]=useState(false);const [matches,setMatches]=useState('');const [minutes,setMinutes]=useState('');const [goals,setGoals]=useState('');const [assists,setAssists]=useState('');const [clubChanged,setClubChanged]=useState(false);const [clubNotes,setClubNotes]=useState('');const [busy,setBusy]=useState(false);const [done,setDone]=useState(false);const [error,setError]=useState('');if(ctx.loading)return <LoadingScreen/>;
 const submit=async(quick=false)=>{if(!ctx.player)return;setBusy(true);setError('');const payload:any={player_id:ctx.player.id,week_start:weekStartISO(),availability_status:quick?'available':availability,fitness_status:quick?'fully_fit':fitness,player_notes:quick?null:notes||null,support_request:quick?null:support||null,club_situation_changed:quick?false:clubChanged,club_situation_notes:quick?null:clubNotes||null,matches_played:quick?null:(matches?Number(matches):null),minutes_played:quick?null:(minutes?Number(minutes):null),goals:quick?null:(goals?Number(goals):null),assists:quick?null:(assists?Number(assists):null),travel_availability:ctx.privateInfo?.travel_availability||null,submitted_at:new Date().toISOString()};const existing=ctx.latestCheckin?.week_start===weekStartISO()?ctx.latestCheckin:null;let q=existing?supabase.from('weekly_checkins').update(payload).eq('id',existing.id):supabase.from('weekly_checkins').insert(payload);const {error:submitError}=await q;if(submitError){setError('We couldn’t send your check-in. Please try again.');setBusy(false);return}setDone(true);await ctx.refresh();setBusy(false)};
 if(done)return <PlayerShell><main className="narrow" style={{paddingTop:74,textAlign:'center'}}><div style={{width:62,height:62,borderRadius:'50%',background:'#e9f7ef',color:'#18794e',display:'grid',placeItems:'center',margin:'0 auto 22px'}}><Check size={28}/></div><h1 className="page-title">Done.</h1><p className="page-intro" style={{margin:'0 auto'}}>DJM has your update. That’s all you need to do this week.</p><a href="/home" className="btn btn-navy" style={{marginTop:28}}>Back home <ArrowRight size={16}/></a></main></PlayerShell>;
 return <PlayerShell inboxCount={ctx.openRequests.length}><main className="narrow player-shell">{error&&<div style={{padding:'12px 14px',borderRadius:14,background:'#fff4f4',color:'#8a1c1c',fontSize:14,fontWeight:650,marginBottom:16}}>{error}</div>}<div className="section-kicker" style={{marginTop:18}}>WEEKLY CHECK-IN</div><h1 className="page-title">60 seconds. Keep DJM current.</h1><p className="page-intro" style={{marginBottom:28}}>If nothing changed, one tap is enough. Otherwise tell us only what matters.</p><button className="quick-good" onClick={()=>submit(true)} disabled={busy}><div><strong>Everything’s good this week.</strong><span>Available, fully fit, nothing important changed.</span></div><ArrowRight size={20}/></button><div className="row" style={{margin:'22px 0',gap:12}}><div style={{height:1,background:'var(--line)',flex:1}}/><span className="tiny muted" style={{fontWeight:700}}>OR UPDATE WHAT CHANGED</span><div style={{height:1,background:'var(--line)',flex:1}}/></div><div className="stack" style={{gap:16}}>
 <section className="check-card"><h2 className="check-question">Are you available?</h2><p className="check-help">Your current playing / move availability.</p><div className="choice-grid">{[['available','Available'],['limited','Limited'],['unavailable','Unavailable']].map(([v,l])=><button key={v} className={`choice ${availability===v?'active':''}`} onClick={()=>setAvailability(v)}>{l}</button>)}</div></section>
 <section className="check-card"><h2 className="check-question">How’s the body?</h2><p className="check-help">No medical detail needed unless you want DJM to know.</p><div className="choice-grid">{[['fully_fit','Fully fit'],['managing','Managing something'],['injured','Injured']].map(([v,l])=><button key={v} className={`choice ${fitness===v?'active':''}`} onClick={()=>setFitness(v)}>{l}</button>)}</div></section>
 <section className="check-card"><h2 className="check-question">Anything changed?</h2><p className="check-help">Club situation, conversations, travel, football, anything you want your agent to know.</p><textarea className="textarea" value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Optional…"/></section>
 <section className="check-card"><h2 className="check-question">Do you need anything from DJM?</h2><p className="check-help">A call, club follow-up, contract question, travel support, anything.</p><textarea className="textarea" value={support} onChange={e=>setSupport(e.target.value)} placeholder="Optional…"/></section>
 <section className="check-card"><button className="row-between" style={{width:'100%',border:0,background:'none',padding:0,textAlign:'left'}} onClick={()=>setDetails(!details)}><div><h2 className="check-question" style={{marginBottom:4}}>Add match details</h2><p className="check-help" style={{margin:0}}>Optional numbers if you want them recorded.</p></div>{details?<ChevronUp/>:<ChevronDown/>}</button>{details&&<div style={{marginTop:22}}><div className="grid2"><div className="field"><label className="label">Matches</label><input className="input" inputMode="numeric" value={matches} onChange={e=>setMatches(e.target.value)}/></div><div className="field"><label className="label">Minutes</label><input className="input" inputMode="numeric" value={minutes} onChange={e=>setMinutes(e.target.value)}/></div><div className="field"><label className="label">Goals</label><input className="input" inputMode="numeric" value={goals} onChange={e=>setGoals(e.target.value)}/></div><div className="field"><label className="label">Assists</label><input className="input" inputMode="numeric" value={assists} onChange={e=>setAssists(e.target.value)}/></div></div><label className="row" style={{marginTop:18,fontSize:14,fontWeight:700}}><input type="checkbox" checked={clubChanged} onChange={e=>setClubChanged(e.target.checked)}/> My club/contract situation changed</label>{clubChanged&&<textarea className="textarea" style={{marginTop:12}} value={clubNotes} onChange={e=>setClubNotes(e.target.value)} placeholder="What changed?"/>}</div>}</section>
 <button className="btn btn-navy btn-block" style={{marginTop:4,minHeight:54}} onClick={()=>submit(false)} disabled={busy}>{busy?'Sending…':'Send check-in'} <ArrowRight size={17}/></button></div></main></PlayerShell>}
